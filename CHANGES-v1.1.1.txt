# Correção v1.1.1

Corrigida a ordem das variáveis em `config/site.env` e
`config/site.env.example`.

`WORK_ROOT` agora é definido antes de `RAW_GFS_ROOT`, eliminando o erro:

    WORK_ROOT: unbound variable

quando `scripts/run_campaign.sh` carrega o arquivo sob `set -u`.


Patch v1.1.2
- Fix campaign inventory rendering for INPUT_MODE=download_gfs. The inventory now lists downloaded GFS GRIB files rather than unused prebuilt WPS FILE paths.
